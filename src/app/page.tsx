import { TimetableWorkspace } from "@/components/TimetableWorkspace";

export default function Home() {
  return (
    <main className="min-h-screen bg-zinc-50 px-4 py-6 font-sans text-zinc-900 lg:px-10">
      <TimetableWorkspace />
    </main>
  );
}
